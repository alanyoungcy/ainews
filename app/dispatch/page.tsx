"use client";

import { useState } from "react";
import { AppShell } from "@/components/app-shell/AppShell";
import { Icon } from "@/components/ui/Icon";

const checks = [
  ["Checksum validation", "Edition assets match the approved visual spec"],
  ["Legal disclaimer", "Capco advisory disclaimer is present"],
  ["Recipient list", "Tier-1 wealth and banking distribution verified"],
  ["Non-public data scan", "No restricted client data detected"],
] as const;

export default function DispatchPage() {
  const [mode, setMode] = useState("Desktop");
  const [checked, setChecked] = useState<string[]>([]);
  const [scheduled, setScheduled] = useState(false);
  const [dispatched, setDispatched] = useState(false);
  const allChecked = checked.length === checks.length;
  const toggle = (label: string) => setChecked((current) => current.includes(label) ? current.filter((item) => item !== label) : [...current, label]);

  return <AppShell><div className="stage-page dispatch-stage"><div className="stage-strip"><span className="stage-label">Editorial pipeline</span><span>/</span><strong>Stage 04: Dispatch &amp; newsletter</strong><span>/</span><span><i className="live-dot" /> HITL Gate 3</span><span className="stage-ref">W42-GENAI-WEALTH-BANKING</span></div><section className="stage-heading"><div><span className="section-kicker">Edition staging workspace</span><h1>Client dispatch &amp; newsletter</h1><p>Preview the approved weekly communication across channels, validate compliance, and authorize the client-ready dispatch.</p></div><div className="stage-heading-actions"><span className="status-chip"><span className={`status-dot ${dispatched ? "green" : "orange"}`} />{dispatched ? "Dispatched" : allChecked ? "Ready for clearance" : "Pending compliance"}</span><button className="button dark" disabled={!allChecked} onClick={() => setDispatched(true)}><Icon name="mail" size={16} />{dispatched ? "Dispatch complete" : "Authorize dispatch"}</button></div></section><div className="dispatch-grid"><section className="dispatch-preview panel"><div className="panel-header"><div><span className="section-kicker">Multi-channel preview</span><h2>GenAI in Tier-1 Wealth &amp; Banking</h2></div><span className="panel-index">Week 42 · Draft</span></div><div className="preview-tabs">{["Desktop", "Mobile", "PDF 1-pager", "HTML source"].map((item) => <button key={item} className={mode === item ? "selected" : ""} onClick={() => setMode(item)}>{item}</button>)}</div><div className={`newsletter-frame ${mode.toLowerCase().replaceAll(" ", "-")}`}><div className="newsletter-top"><span>CAPCO INTELLIGENCE</span><span>W42 / 2026</span></div><div className="newsletter-hero"><div><span className="newsletter-kicker">WEEKLY ADVISORY SIGNALS</span><h3>GenAI is becoming an operating model decision.</h3><p>Across wealth and banking, the practical question is shifting from which model to use to which decisions can safely move into a governed workflow.</p></div><div className="newsletter-art"><span>CAPCO<br /><em>AI</em></span><i /><b /></div></div><div className="newsletter-rule" /><div className="newsletter-columns"><div><span className="newsletter-number">01</span><strong>Agentic workflows move from demo to operating model</strong><p>Define decision rights, escalation paths, and controls before scaling.</p></div><div><span className="newsletter-number">02</span><strong>Sovereign AI becomes a board-level constraint</strong><p>Map model dependencies against jurisdiction and resilience.</p></div><div><span className="newsletter-number">03</span><strong>Efficiency becomes an experience metric</strong><p>Measure latency, cost, and fallback behaviour together.</p></div></div><div className="newsletter-foot">Source: Capco AI Intelligence Workspace · Reviewed for client distribution</div></div><div className="preview-status"><span><i className="status-dot green" />Responsive layout validated</span><span>{mode} view · Hero infographic embedded</span></div></section><aside className="dispatch-side"><section className="export-panel panel"><div className="panel-header"><div><span className="section-kicker">Export hub</span><h2>Client-ready formats</h2></div></div><button className="export-row" onClick={() => setScheduled(true)}><span className="export-icon"><Icon name="download" size={17} /></span><span><strong>PowerPoint deck</strong><small>Editable vectors · 16:9 slide</small></span><Icon name="arrow" size={15} /></button><button className="export-row" onClick={() => setScheduled(true)}><span className="export-icon"><Icon name="download" size={17} /></span><span><strong>Executive PDF</strong><small>Print-ready · 300 DPI</small></span><Icon name="arrow" size={15} /></button><button className="export-row" onClick={() => setScheduled(true)}><span className="export-icon"><Icon name="external" size={17} /></span><span><strong>Responsive HTML</strong><small>Newsletter source · tracked links</small></span><Icon name="arrow" size={15} /></button>{scheduled && <div className="export-toast"><Icon name="check" size={14} /> Export jobs queued for this edition.</div>}</section><section className="compliance-panel panel"><div className="panel-header"><div><span className="section-kicker">HITL Gate 3</span><h2>Security &amp; compliance</h2></div><span className="panel-index">{checked.length}/4 passed</span></div>{checks.map(([label, detail]) => <button key={label} className={`check-row ${checked.includes(label) ? "checked" : ""}`} onClick={() => toggle(label)}><span className="check-box"><Icon name="check" size={13} /></span><span><strong>{label}</strong><small>{detail}</small></span><em>{checked.includes(label) ? "Pass" : "Review"}</em></button>)}</section></aside></div><div className="handoff-bar"><div><strong>{allChecked ? "4 of 4 checks passed" : `${checked.length} of 4 checks passed`}</strong><span>Week 42 client dispatch</span><small>{dispatched ? "Dispatch authorized and queued" : "Complete the compliance audit before authorizing delivery"}</small></div><div><button className="button utility" onClick={() => setChecked(checks.map(([label]) => label))}>Pass all checks</button><button className="button dark" disabled={!allChecked} onClick={() => setDispatched(true)}>{dispatched ? "View dispatch log" : "Authorize & send"}<Icon name="arrow" size={16} /></button></div></div></div></AppShell>;
}
